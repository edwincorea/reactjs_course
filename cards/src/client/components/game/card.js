import "./card.scss";

import React from "react";

const Card = (props) => {
    const {isSelectable = false, onClick, type, card, style = "default", canZoom = false} = props;
    return <p>CARD</p>;
};

export default Card;